# Utsuwa (default VRM)

Bundled default 3D avatar from [Aikeya](https://github.com/aikeyaorg/aikeya) (`static/models/utsuwa.vrm`), MIT licensed.

Shared VRMA animations now live in `models/animations/vrm/` and are available to every VRM model.
